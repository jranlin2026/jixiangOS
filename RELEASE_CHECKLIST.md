# 极享OS 发布验收清单

> 验收日期：2026-07-12
> 分支：`main`
> 提交基线：`8ee2bef`（稳定性修复提交，与 `main` 现有功能合并验收）

## 发布结论

**C. 当前仍不稳定，不能发布。**

## 自动门禁

| 项目 | 结果 | 证据 |
|---|---|---|
| 全新安装 | 通过 | `pnpm install --frozen-lockfile`，安装 474 个包 |
| API / Web 启动 | 通过 | API 3001、Vite 3003 从干净进程启动 |
| TypeScript | 通过 | `pnpm exec tsc -b --pretty false` 退出 0 |
| Lint | 未配置 | `package.json` 无 lint 脚本 |
| 测试 | 通过 | `pnpm test`，125 个测试文件通过 |
| 生产构建 | 通过 | `pnpm run build` 退出 0；保留大 chunk 警告 |
| Prisma schema | 通过 | `prisma validate` 通过 |
| Prisma migration | 失败 | 两个迁移均未登记，且库中不存在 `_prisma_migrations` |
| 生产配置检查 | 失败 | 默认管理员密码不满足生产强度要求 |
| 空库首次启动 | 未验证 | QA 数据库账号无 CREATE DATABASE 权限 |

## 核心流程

| 流程 | 结果 | 说明 |
|---|---|---|
| 登录与未登录拦截 | 通过 | 未登录客户接口返回 401 |
| 管理员组织设置 | 通过 | 页面正常展示，无白屏 |
| 销售访问系统设置 | 通过 | 重定向到无权限页 |
| 只读角色写客户 | 通过 | 接口返回 403，页面无新增/分配/放弃按钮 |
| 财务读取分账 | 通过 | 财务接口可读，设置接口返回 403；已检查页签未出现保存告警 |
| 订单提交与并发审核 | 通过 | 同一申请并发审核只产生 1 订单、1 提成、1 交付 |
| 客户重复提交 | 通过 | 同手机号并发请求返回 201 与 409，数据库只保留一条 |
| 客户空值校验 | 通过 | 空姓名/空手机号返回 400，超长姓名被拒绝 |
| 订单非法金额 | 通过 | 负金额和空订单均返回 400 |
| 不存在客户更新 | 通过 | 返回 404 |
| 完整退款挽回链路 | 未完整验证 | 仍有编辑、审核、分账、删除路径未完成记录级收口 |
| 升单与原销售归属 | 未完整验证 | 本轮没有完整走通 |

## 发布前必须完成

- [x] 修复 `server/services/assetCommandService.ts` 的 5 个 TypeScript 错误，使类型检查和生产构建通过。
- [x] 对客户手机号建立服务端规范化、历史查重和确定性并发唯一键。
- [x] 服务端拒绝空姓名、空手机号及超长姓名。
- [ ] 为现有数据库建立并核验 Prisma migration baseline。
- [ ] 配置符合要求的生产管理员密码并让 `prod:check` 通过。
- [ ] 增加 lint 命令并纳入 CI。
- [ ] 使用具备建库权限的隔离环境验证空库迁移、初始化和首次启动。
- [ ] 完整回归退款挽回、升单、所有员工角色和浏览器异常网络场景。
- [ ] 完成生产备份、校验和、异库恢复演练与数据对账。
